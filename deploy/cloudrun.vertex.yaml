# Private first deployment. Replace IMAGE_DIGEST and VERTEX_MODEL before applying.
# Secret versions and the runtime service account must already exist.
# Initial creation only: services replace can remove existing Hosting pin tags.
# For updates, use services update --image and preserve traffic/tag configuration.
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: hcr-api
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/maxScale: '1'
    spec:
      serviceAccountName: hcr-runtime@hosininaruhito-20260920.iam.gserviceaccount.com
      containerConcurrency: 1
      timeoutSeconds: 50
      containers:
        - image: IMAGE_DIGEST
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: '1'
              memory: 512Mi
          env:
            - name: HCR_BIND
              value: 0.0.0.0
            - name: HCR_PLANNER_PROVIDER
              value: vertex
            - name: VERTEX_PROJECT_ID
              value: hosininaruhito-20260920
            - name: VERTEX_LOCATION
              value: global
            - name: VERTEX_MODEL
              value: VERTEX_MODEL
            - name: HCR_AUTH_MODE
              value: firebase
            - name: FIREBASE_PROJECT_ID
              value: hosininaruhito-20260920
            - name: FIREBASE_APP_ID
              value: FIREBASE_APP_ID
            - name: FIREBASE_WEB_API_KEY
              value: FIREBASE_WEB_API_KEY
            - name: RECAPTCHA_SITE_KEY
              value: RECAPTCHA_SITE_KEY
            - name: HOSHIMIRU_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hoshimiru-api-token
                  key: '1'
